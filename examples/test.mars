struct InnerStruct {
    value: str,
}

struct MyStruct {
    inner: InnerStruct,
}

fn main() -> i64 {
    var my = MyStruct {
        inner: InnerStruct {
            value: "some string",
        },
    };

    var inner = my.inner;

    println(inner.value);

    return 0;
}